// Shared, public, stable constants used across server and client.

/**
 * Canonical privacy policy URL. Single source of truth shared by the consent
 * UI (EmailGate) and the Keap opt-in reason, so the two cannot drift (spec 008,
 * FR-007). Published policy on the WGS shop.
 */
export const PRIVACY_POLICY_URL =
  'https://shop.worshipguitarskills.com/pages/privacy-policy';
